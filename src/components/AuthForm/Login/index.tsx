interface Props {
  register: Boolean;
  setRegister: Function;
  setVisible: Function;
}

const LoginForm: React.FC<Props> = ({
  register,
  setRegister,
  setVisible,
}: Props) => {
  return <></>;
};

export default LoginForm;
